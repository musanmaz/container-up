apiVersion: template.openshift.io/v1
kind: Template
labels:
  template: httpd-hello-web
objects:
- apiVersion: build.openshift.io/v1
  kind: BuildConfig
  metadata:
    labels:
      app: httpd-hello-web
      app.kubernetes.io/component: httpd-hello-web
      app.kubernetes.io/instance: httpd-hello-web
    name: httpd-hello-web
  spec:
    output:
      to:
        kind: ImageStreamTag
        name: httpd-hello-web:latest
    source:
      contextDir: httpd-hello-web
      git:
        uri: https://github.com/musanmaz/container-up
      type: Git
    strategy:
      dockerStrategy:
        dockerfilePath: Dockerfile.openshift
        from:
          kind: ImageStreamTag
          name: httpd:2.4
          namespace: openshift
      type: Docker
    triggers:
    - type: ImageChange
- apiVersion: image.openshift.io/v1
  kind: ImageStream
  metadata:
    labels:
      app: httpd-hello-web
      app.kubernetes.io/component: httpd-hello-web
      app.kubernetes.io/instance: httpd-hello-web
    name: httpd-hello-web
- apiVersion: route.openshift.io/v1
  kind: Route
  metadata:
    labels:
      app: httpd-hello-web
      app.kubernetes.io/component: httpd-hello-web
      app.kubernetes.io/instance: httpd-hello-web
    name: httpd-hello-web
  spec:
    port:
      targetPort: 8080-tcp
    to:
      kind: Service
      name: httpd-hello-web
      weight: 100
    wildcardPolicy: None
- apiVersion: apps.openshift.io/v1
  kind: DeploymentConfig
  metadata:
    labels:
      app: httpd-hello-web
      app.kubernetes.io/component: httpd-hello-web
      app.kubernetes.io/instance: httpd-hello-web
    name: httpd-hello-web
  spec:
    replicas: 1
    selector:
      deploymentconfig: httpd-hello-web
    strategy:
      type: Rolling
    template:
      metadata:
        labels:
          deploymentconfig: httpd-hello-web
      spec:
        containers:
        - image: httpd-hello-web
          imagePullPolicy: Always
          name: httpd-hello-web
          ports:
          - containerPort: 8080
            protocol: TCP
          - containerPort: 8443
            protocol: TCP
        restartPolicy: Always
    triggers:
    - type: ConfigChange
    - imageChangeParams:
        automatic: true
        containerNames:
        - httpd-hello-web
        from:
          kind: ImageStreamTag
          name: httpd-hello-web:latest
      type: ImageChange
- apiVersion: v1
  kind: Service
  metadata:
    labels:
      app: httpd-hello-web
      app.kubernetes.io/component: httpd-hello-web
      app.kubernetes.io/instance: httpd-hello-web
    name: httpd-hello-web
  spec:
    ports:
    - name: 8080-tcp
      port: 8080
      protocol: TCP
      targetPort: 8080
    - name: 8443-tcp
      port: 8443
      protocol: TCP
      targetPort: 8443
    selector:
      deploymentconfig: httpd-hello-web
    type: ClusterIP
parameters:
- description: Name that will be given to all the resources
  displayName: Name
  name: NAME
  required: true
  value: mywebsite
